面向对象+多线程

每次实例化对象时，都是最开始创建的那个对象，不再重复创建对象【用的同一个内存地址】
第一种：类的魔术方法【不需要人工调用，在特定的时刻自动执行】
触发时机，作用，参数，返回值，注意事项
1、__init__初始化方法
属性(成员)，方法
实例化对象后触发init
one = Human() #实例化对象【1、创建一个对象2、为对象初始化操作】
print(one.__dict__)#查看对象属性（成员）

2、__new__构造方法
one = Human() #实例化对象【1、创建一个对象(new)  2、为对象初始化操作(init)】
触发时机：实例化对象时触发
作用：管理控制对象的生成过程

